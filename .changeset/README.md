# Changesets

This directory stores release notes and version intent files created with:

```bash
pnpm changeset
```

Each changeset file should describe user-facing changes and include the correct bump type.
